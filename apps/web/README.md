# STT Web (Vite + React)

This is the frontend for **STT — Simple Time Tracking**.

## Local dev

```bash
cd apps/web
npm install

cp .env.example .env
npm run dev
```

### Config

`apps/web/.env`:

```env
VITE_API_BASE_URL=http://localhost:8000
```

## Build

```bash
cd apps/web
npm run build
npm run preview
```
